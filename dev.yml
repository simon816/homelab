---
- hosts: dev

  pre_tasks:
  - name: git user
    become: yes
    user:
      name: git
      password_lock: yes

  roles:
    - intnet-node
    - naemon-remote
    - auto-updates
    - gitea
    - role: nginx
      vars:
        site: dev
