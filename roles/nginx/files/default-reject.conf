# Reject any request with invalid Host
server {
    listen      80 default_server;
    listen      [::]:80 default_server;
    listen      443 default_server ssl http2;
    listen      [::]:443 ssl http2 default_server;
    server_name _;

    include /etc/nginx/conf.d/ssl.conf;

    return 444;
}
